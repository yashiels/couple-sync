import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts without crashing', (WidgetTester tester) async {
    // Smoke test — verifies basic setup
    // Firebase must be mocked for full integration tests
  });
}
