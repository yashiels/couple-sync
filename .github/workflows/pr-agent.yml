name: PR Agent
on:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize]
  issue_comment:
    types: [created]
permissions:
  issues: write
  pull-requests: write
  contents: read
jobs:
  review:
    uses: skynergroup/.github/.github/workflows/pr-agent.yml@6468ab0a5ca130879c05ef3985957bddcfc0f43f
    secrets:
      KIMI_API_KEY: ${{ secrets.KIMI_API_KEY }}
